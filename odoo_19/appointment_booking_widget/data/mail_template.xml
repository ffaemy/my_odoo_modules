<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="appointment_booking_email" model="mail.template">
            <field name="name">Appointment Booking Confirmation</field>
            <field name="model_id" ref="model_appointment_booking"/>
            <field name="subject">Thanks for your appointment request ({{ object.name }})</field>
            <field name="email_from">${(user.email_formatted or 'info@example.com')}</field>
            <field name="email_to">${object.partner_email}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
<p>Hi ${object.partner_name or 'there'},</p>
<p>Thanks for submitting an appointment request. Weve logged your reference <strong>${object.name}</strong>.</p>
<p>
    <strong>Preferred date/time:</strong> ${format_datetime(object.preferred_datetime, tz=object.env.user.tz) or 'Not provided'}<br/>
    <strong>Phone:</strong> ${object.phone or 'Not provided'}<br/>
    <strong>Notes:</strong><br/>
    ${object.notes or 'No notes entered.'}
</p>
<p>We’ll confirm or adjust the slot shortly.</p>
                </div>
            </field>
        </record>
    </data>
</odoo>
